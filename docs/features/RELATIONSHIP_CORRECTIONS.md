## Installed native exact-credit hide and history restore — 2026-09-08

Actual installed Desktop/WebView on the generated four-work fixture passed explicit spoiler consent, source-credit selection, draft/preview/save, forward and reverse role isolation, and history-based restore. For Generated1 (manually bound to v1 solely as a fixture), only s804/aid1433/chardesign was hidden; s804/aid1433/art remained on the work and person pages. Existing private-staff correction and broad hidden-person decision stayed in place. History Use previous state then Review/Save restored both source roles through revision3, retaining revisions1/2 and their receipts. Independent read-only SQLite reconciliation verifies revisions1→2→3, exact original correction payload/index after restore, and two appended history/receipt rows. Evidence: test-output/mvp-goal/native-editor-001/{report.json,verify.py,before.json,hide-saved.json,undo-saved.json} and retained UI trees. Source metadata refreshed automatically on viewing; raw-cache immutability and human work truth are not claimed.

Native add/rebind/candidate/alias, conflict/lost-response/offline cases and reverse-page verification after undo remain open. Add form was opened but never saved. WebView option clicks were rejected by the input helper as Chrome Legacy Window instead of Galroon; keyboard selection reached Staff credit. A separate Internet Download Manager dialog then obscured the form. No action was taken on that unrelated app; user was asked to handle it. Desktop/Core remain running for this isolated fixture pending native resumption; this is not a clean shutdown milestone. No runtime/package change: current task-messages-package and prior271 Core/85 frontend/8 controller evidence remain unchanged. All other M0–M6 and numbered gates stay open as previously recorded; Windows MVP is incomplete.

## Schema40 separate Core and release-scale acceptance — 2026-09-07

Rebuilt debug and optimized release Core40, product version still0.0.1; installed35 and historical installed15 are unchanged. Binary identities are saved in `test-output/mvp-goal/schema40-binaries.json`: current release SHA256 `032ee802ee8beffa4e7c4890674783becda63af870000c5d8f3b61a20413e208`; debug `d77a32e66645879f85c9e95c4afe79cd12eec517caee9ec3255ea18120e24c85`. Earlier schema40 debug HTTP runs used2000545e... and remain separate evidence.

Current release passed `editor-release-schema40-r1/report.json`: exact add/rebind/hide/undo, actual alias, forward/reverse spoilers, stale preview rejection, receipt/history across restart and backup/offline restore, Web private routes403 and unchanged raw cache. It also passed NEW `verify_managed_http` / `managed-release-schema40-r1/report.json`: generated source file, competing-root approval rollback, real organize/undo, organizing facts unorganized→pending→organized→pending→unorganized, pinned smart page after move/restart/restore, HTTP backup export/local restore, restored active-root enforcement and acquisition to a separate destination without changing source organizing status. Source hash unchanged after reviewed undo/copy. These are separate executable HTTP checks, not installed Desktop UI.

The initial debug100000-work compute exceeded the verifier's20-second request deadline (`smart-native-schema40-100k-r1`); after rule-directed projection, debug with the organizing condition also exceeded it (`r2`). Both failures and cancellation logs are retained; neither is labelled passed. Smart evaluation now derives required fields once per rule and omits unused organization, personal-tag, source-location, relation and edition queries. Related-tag dependencies still load their required relation projection; direct all-facts access remains available. Full Core265/265 passed after this change (`smart-projection-core-tests.log`). A read-only2000-work diagnostic found larger statement caches did not help; cache capacity was not changed.

The actual release build passes the unchanged20-second/300ms checks with the organizing predicate explicitly included alongside status, relation tags/brand and same-edition language/availability: `smart-release-schema40-100k-r1/report.json`.100000 generated works each have cached metadata, edition and resource/file records, but zero physical game files. Manual compute4337ms; concurrent result read2ms;2000 page reads p95=2ms; full100000-member conversion3139ms; automatic update6654ms; pinned pages and idempotent conversion survive restart. This establishes release smart-list performance, not a claim that the debug deadline regression is fixed or that every collection interaction scales.

Machine: i7-14650HX,16 cores/24 threads,68413153280 bytes RAM, Windows10.0.26200.20 process samples recorded max observed working set415203328 bytes, OS lifetime peak working set520175616 bytes (about496MiB), private bytes421576704; `memory-summary.json`, `memory-samples.json`, `machine.json` beside the release-scale report. RSS acceptance threshold remains unagreed. Frontend84/build evidence unchanged. All validation Cores stopped through their identity handshake; verified no galroon-core.exe after completion. Full installed/product-version upgrade, broad catalog UI/RSS/cold-start, human matching/reference gates, remaining file faults and other M0–M6 requirements remain. Goal active.

# Exact relationship correction implementation contract

Status: exact storage/index/projection, forward/reverse/smart/search integration and the owner/Desktop candidate/preview/commit/history APIs and editor are implemented and included in current source/release/installed0.0.2/schema41. The earlier Core38/248 and frontend84 component evidence remains historical; latest complete suites are Core271/frontend85 (MVP_STATUS.md). Installed0.0.2/schema40 HTTP correction/extended restore evidence is retained in extended-installed-002-r3 and the release editor report above. Installed native exact staff hide/preview/save/history restore and forward/reverse role isolation now pass (native-editor-001). Native add/rebind/candidate/alias and fault cases remain pending; this is partial editor acceptance.

## Identity and edits

A correction has a stable local ID, optional exact source edge to replace, and optional local edge. Add = local edge only; hide = source only; rebind = both. Source keys preserve relationship kind and role/alias/note, so editing one voice alias cannot erase another voice credit or the same person's scenario credit. People retain stable VNDB IDs separately from actual credit names. Voice edges identify both person and character and retain alias ID. User-provided names are plain text labels, never inferred provider identities. Unknown spoiler level must be selected as2 until reviewed; UI must not silently default it to safe.

Existing broad hidden-person/character/company/work decisions remain intact. New exact corrections must compose with them consistently; broad hidden decisions take precedence. Do not discard old hidden decisions when a legacy client edits. Reject ambiguous duplicate source replacements/local keys and cross-kind substitutions. Input capped200, labels300 characters, notes500; no arbitrary image URLs or HTML.

## Implementation contract (runtime steps1–3 now implemented; installed acceptance remains)

1. Versioned storage, CAS revision, idempotent receipt digest and before/after history in one transaction, including legacy-state compatibility and backup/restore. Read/write restricted to owner/paired Desktop; Web sees only projected results.
2. Apply corrections to a copy of raw cached provider data, never overwrite it. Explicit provenance identifies local edges. Reverse person/character/company pages and search/list membership must apply identical edges. Account for manual-only works separately from provider continuation: never truncate or repeat pages; keep unverified scope incomplete.
3. Resolve candidate IDs using available entity/profile data, preview actual name and alias, explicitly choose role and spoiler level. For rebind preserve selected source key. No implicit global person merge. UI review must show before/after, handle stale revision and ambiguous response, and restore prior state through new revision.
4. Test add/hide/rebind/undo in both directions, multi-alias/multi-role credits, refresh/offline, private-data restoration, Web403, spoiler DOM/accessibility and installed flow. Only then mark manual relationship corrections complete.

## Historical implementation checkpoints (superseded by current status)

Initial validation evidence: targeted relation_corrections tests; this module has no runtime caller yet. Installed schema35 package remains unchanged.

Storage validation: full212 Core tests passed. Exact corrections, CAS, receipt digest, history and undo through a new revision persist across reopen. Injected receipt trigger failure rolls back decision/history.50-item history pagination and separate legacy hidden rows verified. Storage module is not exposed to runtime clients until projection/API integration is ready. Installed schema35 unchanged.


Forward projection3 tests passed: exact voice alias rebind keeps other aliases/staff, preserves raw source/timestamp, is idempotent; all edge kinds carry local provenance/spoiler; broad hidden decisions override additions; missing work identity refuses projection. Before exposing results, frontend must consume per-edge spoiler for staff/company/work/voice, and reverse membership must include manually added work edges. This helper is intentionally not called by existing handlers yet.


Reverse loaded-page helper now applies exact corrections to provider profile rows; last credit removal filters a work, other duties retain it, company filtered rows supply known current-company identity and character removal updates loaded appearance list. Unknown old person credit arrays retain the row with local_relationships_incomplete=true. Provider page/more/stale are preserved, empty filtered pages retain continuation_verified. Manual-only works and search/list indexing remain unimplemented; handlers remain unwired. Five projection tests passed; relation-reverse-tests.log. Before runtime wiring combine existing broad profile projection and enforce incomplete flag in list-scope consumers.


Schema37 adds relation_correction_index.rs: indexed candidate work lookup for source/destination targets, atomic with correction/history/receipt;50 IDs per keyset page with global correction revision required on continuation. Changed revision rejects continuation. Undo removes indexed candidates; replay leaves revision unchanged. Migration backfills schema36 rows. Candidates are NOT membership/visibility claims. Manual-only work metadata resolution and UI/API composition remain required. Full220 Core tests passed (schema37-core-tests.log).


manual_profile.rs now resolves a bounded manual-membership page from reverse-index candidates. Projects explicit local corrections first, applies broad hiding/spoiler visibility before metadata lookup, uses existing active local titles or cached external work title, never inserts a work. Visible missing metadata is partial with missing_work_ids; hidden missing metadata does not disclose IDs. Returns manual_credit_subset, separate next/revision and only manual staff/voice credits. Caller must merge credits by exact identity instead of replacing full provider work rows. Three targeted tests passed. Runtime API, stream merge, UI and full privacy/List acceptance remain pending.


Read API now wired: GET /api/entities/{s|c|p ID}/manual-works with after/revision/spoilers query. Anonymous401, authenticated readonly Web GET, Web POST403, invalid cursor400 and no private notes verified in router tests. Correction writes remain unexposed. src/profileCredits.ts merges manual credit subsets by exact person/alias/role/note or voice person/alias/character/note; retains full provider metadata and unknown fields, no input mutation. Two merge tests pass. Still wire EntityProfile paging. Before exposing staff rebind writes, extend Rust Staff Key to include optional aid (currently lacks it); otherwise same-role multiple staff aliases would be conflated. Frontend merge already preserves aid.


## Current follow-up: Staff aid (2026-09-07)
Staff keys now include optional aid. Missing/null aid matches only a missing/null provider aid, never all aliases. Negative IDs are rejected. None is omitted on serialization to preserve pre-aid receipt digests; old stored corrections/history remain readable without a schema bump. Same-person/same-role aliases survive exact hide/rebind in forward and loaded reverse projections, and manual-page output retains aid. Full Core library regression: 228 passed, 0 failed (`test-output/mvp-goal/staff-aid-core-tests.log`). This supersedes the historical missing-aid warning above. UI/write integration and all other gates remain pending; no executable/package rebuild in this follow-up.

## Current follow-up: reverse profile runtime/UI (2026-09-07)
`visible_profile_page` now combines broad hiding, exact corrections, per-edge spoilers and complete-character-appearance projection in people/characters/companies handlers. `relationship_revision` guards traversal; broad edits also advance the shared revision once per new receipt. `manual_profile` supplies projected membership evidence so manual-only character works do not depend on the provider appearance array. EntityProfile consumes both streams through `useProfilePages`, preserving credit aliases and nested provider metadata, independent paging/retry/abort, stale introduction/works, query-scope invalidation and revision restart. `completeProfileScope` traverses both sources at one revision and checks it again at the end; partial, missing metadata, stale provider data, incomplete exact projection and repeating cursors refuse full conversion. Web remains readonly.
Evidence: Core232, frontend82, production build; output/playwright/profile-stream-ui-report.json (6 generated Edge component scenarios plus independent aborts); test-output/mvp-goal/profile-stream-native-20260907-r3/report.json (separate rebuilt Core37, HTTP/restart/Web403, untouched source cache, backup/restore receipt/history/index/undo). Backup intentionally excludes provider cache: missing metadata is explicitly partial after restore until rehydrated. Exact write UI/API and full forward/search/smart scope remain open. Installed35 unchanged.

## M4 forward projection and smart relation consistency — 2026-09-07

Work and reference-work HTTP handlers now share exact+broad effective projection and per-edge spoiler filtering. Synthesized voice appearances choose the least revealed local appearance independent of edit order; explicit character appearances dominate nested voice copies. WorkExperience/ReferenceWork use an abortable query-scope hook and remove prior-scope data immediately; matched works no longer fall back to raw hidden developer text.

Smart facts, related personal Tags and value pickers share the same effective corrections, including safe manual positives without provider cache. Hidden/missing metadata remains unknown. Schema38 adds correction-table dirty tracking and invalidates older smart snapshots once; committed add/spoiler change/undo recompute results while pinned snapshots stay immutable.

Validation: full Core237/237 (`test-output/mvp-goal/forward-core-tests.log`), current frontend82/82, production build. Four generated Edge component scenarios (local/reference x1200/390) passed spoiler DOM, retry, raw fallback suppression, no overflow and reference inactive abort (`output/playwright/work-projection-ui-report.json`); screenshots inspected. Separate rebuilt Core38 passed reference forward aliases/spoilers, reverse pages, Web readonly rejection, restart, six unchanged raw caches, backup/restore receipt/history/index/undo (`test-output/mvp-goal/forward-native-20260907-r1/report.json`). All test data generated; zero collection works/source files in native fixture. Installed35 remains unchanged. Search exact/manual membership, write API/editor and remaining M0–M6 gates stay open; Goal active.


## M4 discovery effective membership and full scope — 2026-09-07

Search now projects exact edits and checks a shared relationship revision across source/manual requests. Company predicates exclude replaced source edges and independently verify locally added producer identities. Trait-work predicates verify the matched characters' actual appearance spoiler levels before local projection; hidden or removed appearances cannot admit a work. Character search cards include saved manual appearances and omit hidden links. Query evidence stays raw in cache; bounded provider pages/chunks preserve explicit partial/stale states. Current query shapes were accepted by the public VNDB API with results=0; [Kana documentation](https://api.vndb.org/kana) confirms trait and appearance spoilers are distinct fields.

GET /api/discover/manual has independent50-candidate keyset paging and revision, uses only visible membership before work metadata lookup, and returns partial for missing metadata. HTTP numeric/boolean parameters use explicit deserialization (flattened serde_urlencoded fields rejected numeric revisions in the first native attempt; fixed and router-tested). Web writes remain403. DiscoverySearch now merges ranked source results with additional saved matches, supports independent cancel/retry, scope clearing, stale accumulation, repeated-page rejection, and completeProfileScope traversal of both streams at one revision including final revision recheck. Cancel or changed scope cannot open an outdated List review.

Also tightened shared privacy: work and reverse voice payloads carry only the relevant work appearance, manual profiles retain source appearance levels, and person-page nested source appearances constrain local voice additions. These fixes preserve raw caches and explicitly selected spoiler scopes.

Evidence: full Core244/244 (`test-output/mvp-goal/discovery-core-tests.log`), frontend84/84 and production build (`discovery-build.log`). Four generated Edge studio/trait x1200/390 scenarios passed (source/manual pages, complete4-work List, immediate spoiler DOM clearing, source/manual outages, revision restart, partial scope guard and readonly390). Separate UI checks passed provider/manual/inactive/List abort, mid-List revision race, stale accumulation and repeated pages (`output/playwright/discovery-stream-ui-report.json`, `discovery-cancellation-ui-report.json`); screenshot inspected. Rebuilt Core38 HTTP validation passed (`test-output/mvp-goal/discovery-native-20260907-r3/report.json`): source/manual search and character appearances, forward/reverse projection, Web403, restart,12 untouched raw caches, backup/restore receipts/history/index/undo. Zero source files/collection works in this generated native fixture. Installed35 unchanged. Exact write API/candidate review/editor and full remaining M0–M6 gates are still open; Goal active.


## M4 exact relationship editor and offline history restore — 2026-09-07

Owner/paired Desktop now have private candidate, correction-state/history, preview and commit APIs. Candidate resolution preserves stable entity IDs and actual staff/voice alias IDs; newly selected source edges must match exactly once. Before/after previews bind the input, source snapshot, broad decisions and resolved identities. Commit checks that fingerprint under the same database lock, then persists the correction/history/index/receipt atomically. Lost-response retries return the original receipt without overwriting subsequent edits. Web private routes return403. Editing hidden credits requires explicit spoiler review.

RelationshipEditor retains broad hiding and now opens ExactRelationshipEditor for add/hide/rebind, resolved identities/aliases, explicit role/appearance visibility, draft review, reload, history and undo. Read/candidate cancellation preserves drafts and cannot commit. Spoiler visibility starts unselected; unknown uses major/unreviewed. A complete historical before-state may be restored with its history revision even after backup removed provider/candidate caches. Edited historical drafts must resolve identities normally; a history reference cannot authorize arbitrary new links.

Evidence: full Core248/248 (`test-output/mvp-goal/editor-core-tests.log`); frontend84/84 (`editor-frontend-tests.log`), production build (`editor-build.log`). Generated Edge1200/390 passes exact rebind with other alias/role preserved, all add kinds, hide, before/after, consent, explicit spoiler selection, lost-response receipt retry, history undo, conflict/reload and no overflow (`output/playwright/exact-editor-ui-report.json`). Read/candidate abort and offline draft retention passed (`exact-editor-cancellation-ui-report.json`); both review screenshots inspected. Rebuilt Core38 HTTP/restart/backup/restore acceptance passed (`test-output/mvp-goal/editor-native-20260907-r2/report.json`), including cached actual alias, add/rebind/hide/undo, forward/reverse spoilers, broad preview conflict, receipt replay after later edits and restart, Web403, unchanged raw cache, and offline full-history restoration through HTTP after backup/restore. All data generated; zero source files/collection works. Current debug SHA256:132c15fe139876c29f15f7f2550c141c5670a5205d74f79fd8167985461485a9. Installed35 unchanged. Native installed WebView/end-to-end, independent real-work/human-label evidence and all remaining M0–M6 gates stay open; Goal active.

