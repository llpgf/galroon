## M2 automatic matching history foundation — 2026-09-07

Added Organize -> Matching history, backed by authenticated GET /api/resources/{id}/matching-history?before=<rowid>. It reads persisted match_items, newest first,20 sequences per page with an exclusive cursor. UI shows recorded source names/revision, algorithm version, candidate title/reason, decision or query failure; older missing source data is explicit. Modal has native focus containment/Escape, page failure retains existing content and retry appends. Candidate prose/images are omitted from the projection; Web sessions are rejected. Matching is explicitly distinct from file integrity and movement.

New decisions preserve source listing fingerprint (not content hash), prepared queries and recorded_at. Provider failure saves the pending item's source/error before pausing. This is the latest result per sequence/resource, not an immutable log of every resume attempt. Repeated-attempt events, manual decisions, persistent issue lifecycle/defer/reopen and independent30 multilingual samples remain M2 work. No threshold or binding protections changed. Existing backups already include match_items; no schema change.

Validation: Core107 passed; final focused projection and real HTTP tests passed (unauthenticated rejection, Web denied, owner empty history, invalid cursor). Frontend43 and TypeScript passed. Browser fixture verifies candidate reasons, older-page error/retry, no horizontal overflow, Escape close. Evidence test-output/m2-match-history/. New frontend/backend is source-only, not in the current installed package. M1 installed UI pairing/fault gates remain open; independent M2 work advanced without marking M1 complete. Goal active; originals/NAS untouched.

## M2 retained matching attempts — 2026-09-07

Matching results/skips and provider failures now append match.attempt events in the same SQLite transaction as item/job changes. Resume can replace the sequence summary without losing earlier failures. Existing events table and a resource/sequence partial index are used; no credential or file-operation changes. Matching history adds All attempts, with exclusive event-sequence cursor paging via attempts=true. Legacy summaries remain separately available; earlier attempts cannot be reconstructed and are not fabricated. The UI preserves native modal/Escape and paging error recovery.

Core109 passed, including two failures then success on the same job, retained order/older cursor, backup/restore equality, and forced event-insert failure rolling back the new work/bindings. TypeScript/frontend43 passed. Browser fixture verifies mode switching, retained failures, Escape and existing paging retry; screenshot inspected. Evidence: test-output/m2-attempts/. This is source/browser evidence, not installed package acceptance. M1 UI pairing/fault cases and M2 persistent issues/manual-decision timeline/independent matching samples remain open; M3–M6 remain intact. Next meaningful M2 work: durable issue lifecycle and retry/defer/reopen integration. Goal active, original resources and NAS unchanged.
